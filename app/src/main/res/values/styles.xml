<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Customize Text Input Layout Style -->
    <style name="LoginTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/colorBlack</item>
        <item name="boxStrokeWidth">@dimen/size_2</item>
        <item name="android:textColorHint">@color/colorBlack</item>
    </style>

    <!-- Customize Text Input Layout Label -->
    <style name="LoginTextInputLayoutLabel" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/colorGray</item>
        <item name="android:textSize">@dimen/text_14</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/colorBlack</item>
        <item name="colorControlNormal">@color/colorBlack</item>
        <item name="colorControlActivated">@color/colorBlack</item>
    </style>

    <!-- General Text Input Layout Style -->
    <style name="GeneralTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="colorAccent">@color/colorGray</item>
        <item name="colorControlNormal">@color/colorGray</item>
        <item name="colorControlActivated">@color/colorGray</item>
    </style>

    <!-- Customize Tab Layout -->
    <style name="Base.Widget.Design.TabLayout" parent="android:Widget">
        <item name="tabBackground">@drawable/bg_tab_selector</item>
        <item name="android:textSize">@dimen/text_12</item>
        <item name="fontWeight">500</item>
    </style>

    <!-- Customize Checkbox -->
    <style name="colored_checkBox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <!-- Checked -->
        <item name="colorAccent">@color/colorRed</item>
        <!-- Default/unchecked -->
        <item name="android:textColorSecondary">#BDBDBD</item>
    </style>

</resources>
